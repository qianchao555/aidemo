## 一、RAG 基础与系统架构

### Q1：这个项目的 RAG 全链路是怎样的？从用户提问到生成回答，经过哪些环节？

**参考回答：**

整个链路分 **前置处理 → 检索 → 生成** 三个大阶段，共 7 个关键节点：

```
用户提问
  │
  ├─ ① FAQ 四级匹配（精确 → 模糊 → 关键词 → 语义向量）
  │    命中 → 直接返回标准答案，绕过 LLM
  │
  ├─ ② 闲聊检测（关键词 + 短文本特征）
  │    命中 → 友好回复，不调用检索
  │
  ├─ ③ 意图分类（6 种正则模式 + FALLBACK 兜底）
  │    PROCESS_GUIDE / POLICY_QA / DEFINITION / COMPARISON / SCOPE_CHECK / VIOLATION
  │
  ├─ ④ 策略注入（Hook 层动态选择策略指令注入 SystemMessage）
  │    流程分步引导 / 精准回答 / 兜底检索
  │
  ├─ ⑤ ReAct Agent 决定调用 searchKnowledge 工具
  │
  ├─ ⑥ 混合检索：向量语义检索 + pg_trgm 关键词 → RRF 融合 → LLM 重排序 → 结构去重
  │    质量评分 → 不足时返回「最高优先级指令」告知 LLM
  │
  └─ ⑦ LLM 基于检索结果 + 策略指令生成最终回答，标注出处
```

**关键设计决策：**
- FAQ 前置拦截：已知高频问题零成本返回，不消耗 LLM token
- 策略分离：基础 Prompt 只有公共规则（~40行），具体策略由 Hook 动态注入，节省 ~60% prompt token
- 意图路由是「标注式」而非「决策式」：Hook 标注意图、注入策略，LLM 读取标注后自行决策，保留灵活性





### Q2：为什么选择混合检索而不是纯向量检索？RRF 融合的原理是什么？

**参考回答：**

**纯向量检索的局限：**
- 向量 embedding 对语义相近但关键词不同的内容检索效果好，但对**精确关键词匹配**（如条款编号 "第3.2条"、专有名词）不够敏感
- 中文制度文档大量使用编号、条款引用，这些精确匹配场景向量检索不如关键词检索

**混合检索方案：**
- **向量检索**：pgvector HNSW 索引，余弦距离，捕捉语义相似性
- **关键词检索**：pg_trgm 三元组模糊匹配，`similarity()` 函数排序，捕捉精确关键词
- 两者各取 topK × 2 候选，扩大候选池

**RRF（Reciprocal Rank Fusion）原理：**

```
RRF_score(d) = 1/(k + rank_vector) + 1/(k + rank_keyword)
```

- k=60 是经典取值，确保排名靠前的文档获得显著更高得分
- rank 从 1 开始，只考虑排序位置而非绝对分数
- 优势：不需要分数归一化，对两个异构检索源的排名直接融合

融合后还有两道后处理：
1. **LLM 重排序**：候选 > 3 时，用 LLM 对每个 chunk 打分（1-5分），取 top 3
2. **结构去重**：同文档同章节的 chunk 只保留排名最高的那条

---



### Q3：RAG 系统中 embedding 模型的选择有哪些考量？这个项目为什么用 text-embedding-v2？

**参考回答：**

**选择 embedding 模型的核心考量：**

| 维度 | 说明 |
|------|------|
| **维度** | 影响检索精度和存储成本。1536 维是平衡点 |
| **中文能力** | 中文制度文档需要中文优化的模型 |
| **最大输入长度** | 制度文档的 chunk 通常 300-500 token，需要支持 |
| **成本与延迟** | 每次 FAQ 语义匹配、文档入库都需要调用 |
| **与 LLM 的配套** | 同厂商的 embedding + LLM 通常有更好的协同效果 |

**本项目选择 text-embedding-v2（1536 维）的原因：**

- DashScope（阿里云）生态配套，与 qwen-plus LLM 协同
- 1536 维是 pgvector HNSW 索引的高效区间
- 中文 C-MTEB 基准表现优异
- 成本可控（批量入库时按 batch size=25 分批调用）

**工程实践：FAQ embedding 缓存（ConcurrentHashMap）**

FAQ 问题的 embedding 向量计算一次后缓存，后续语义匹配直接复用，避免重复调用 API。





## 二、Agent 架构

### Q4：这个项目用的是 ReAct Agent，解释一下 ReAct 模式的核心思想，以及在本项目中具体如何体现？

**参考回答：**

**ReAct = Reasoning + Acting**，核心思想是 LLM 在**思考（Thought）→ 行动（Action）→ 观察（Observation）** 的循环中解决问题。

```
Thought: 用户问年假怎么算，我需要查制度文档
Action: searchKnowledge("年假 计算 天数")
Observation: [检索到 3 条相关文档...]
Thought: 第1条文档提到工龄1-10年有5天年假，第2条提到...
Action: 生成最终回答
```

**本项目中的体现：**

1. **工具定义**：`searchKnowledge` 是 Agent 唯一工具，由 `KnowledgeRetrievalTools` 实现，通过 `@Tool` 注解注册

2. **Agent 决策权**：Agent 自行决定**何时调用工具**、**传什么参数**、**是否需要二次检索**。Hook 层不强制检索，只注入策略引导

3. **工具返回的指令优先级**：当检索质量不足时，工具返回 `【系统指令-最高优先级】` 格式的消息，Agent 的 System Prompt 要求**原样输出**回复模板，这是 Agent 架构中的「硬约束」机制

4. **多轮对话**：`RagQaAgentService.buildEnrichedQuestion()` 注入最近 5 轮对话历史，Agent 在上下文中理解追问

**ReAct vs 传统 RAG 的区别：**
- 传统 RAG：检索 → 生成，一次性
- ReAct RAG：思考 → 检索 → 观察 → 可能再检索 → 生成，支持多步推理

---

### Q5：Agent 的 Hook 机制在这个项目中起了什么作用？为什么选择在 Hook 中做意图路由而不是在 Agent 的 System Prompt 中做？

**参考回答：**

**Hook 机制的作用：**

Spring AI Alibaba 的 `MessagesModelHook` 允许在 Agent 调用 LLM **之前**拦截和修改消息列表。本项目在 `@HookPosition.BEFORE_MODEL` 时机做：

1. FAQ 命中 → 注入标准答案（直接短路 Agent 交互）
2. 闲聊检测 → 注入闲聊指令
3. 流程续接检测 → 注入续接指令
4. 意图路由 → 注入完整策略指令

**为什么不在 System Prompt 中做意图判断？**

| 维度 | System Prompt 中做 | Hook 中做 |
|------|-------------------|----------|
| **Token 消耗** | 所有策略必须写在 Prompt 里，每次调用都消耗 | 只注入匹配的那一条策略，节省 ~60% token |
| **可靠性** | LLM 可能忽略 Prompt 中的分类指令 | 正则匹配是确定性的，100% 一致 |
| **可维护性** | 改一条策略要动整个 Prompt | 策略独立常量，改流程不影响制度问答 |
| **短路能力** | LLM 必须先读完 Prompt 再决策 | FAQ/闲聊可直接返回，零 LLM 调用 |

**关键设计原则：Hook 负责「标注」，LLM 负责「决策」**

Hook 告诉 LLM "你面对的是什么类型的问题，用什么策略回答"，但不强制 LLM 的每一步行为。FALLBACK 时 LLM 仍然可以自行判断是否需要检索——这正是 Strategy Separation 设计文档的核心思想。







## 三、意图识别与路由

### Q6：意图分类用了正则匹配，为什么不用 LLM 做意图识别？各自的优劣是什么？

**参考回答：**

**选择正则的原因：**

1. **零成本零延迟**：正则是 O(n) 字符串匹配，不消耗 token，不增加延迟
2. **确定性**：相同输入永远得到相同分类，不会因为 LLM 温度参数产生不一致
3. **可调试**：加一条规则、修一个 pattern，效果立即可验证
4. **制度问答场景适配**：用户提问有明确的 trigger word（请假、报销、流程、第X条），正则覆盖率高

**正则的局限：**
- 覆盖率永远不是 100%，未覆盖的合法问题会走到 FALLBACK
- 多义性问题（"加班制度怎么申请" 同时匹配 POLICY_QA 和 PROCESS_GUIDE）
- 无法理解语义变体（"我想请个假" vs "请假流程"）

**如果用 LLM 做意图识别：**
- 优势：语义理解更准确，覆盖面更广
- 劣势：每次多一次 LLM 调用（~200-500 token），增加 500ms-1s 延迟
- 折中方案：正则为主力，LLM 为兜底（正则未命中时让 LLM 判断是否需要检索）

**当前项目的选择是务实的**：正则匹配命中率在制度问答场景下可达 70-80%，命中时零成本路由，未命中时 FALLBACK 走兜底检索（已从"反问澄清"改为"先检索再回答"）。







### Q7：FALLBACK（兜底检索）策略的设计经历了什么演变？当前方案的考量是什么？

**参考回答：**

**演变过程：**

| 阶段 | FALLBACK 行为 | 问题 |
|------|-------------|------|
| v1（设计稿） | 反问用户最多 2 个问题，**禁止调用 searchKnowledge** | 大量合法问题正则未命中（"年假有多少天？"），直接拒绝检索，用户体验差 |
| v2（当前） | 先调用 searchKnowledge，基于结果回答，结果为空时如实告知 | 只有检索结果为空时才提示用户更换关键词 |

**当前设计的考量：**

```
兜底检索策略：
1. 先调用 searchKnowledge 检索
2. 基于检索结果给出精准回答，标注出处
3. 检索为空 → 如实告知，建议更换关键词或联系 HR
4. 问题极模糊 → 可反问最多 1 个关键问题（但先尝试检索）
```

**核心洞察**："正则未命中 ≠ 问题无意义"。正则覆盖率有限是已知约束，FALLBACK 应该是「用检索兜底」而不是「用反问兜底」。检索本身就是最好的意图澄清——检索结果会自然暴露问题是属于制度类还是流程类。









## 四、Prompt Engineering

### Q8：这个项目做了「策略分离」——把策略指令从基础 Prompt 中拆出来由 Hook 动态注入。为什么要这样设计？带来了什么好处？

**参考回答：**

**设计动机（"Strategy Separation" 设计文档）：**

原版 `INTERACTIVE_SYSTEM_PROMPT` 是 ~100 行的单体 Prompt，包含：
- 6 种意图的策略说明
- 流程分步引导的完整指令
- 精准回答策略
- 引导澄清策略

**问题：**
1. 每次 LLM 调用都要处理全部策略，但实际只用一种
2. 流程指引请求看到「精准回答策略」，制度问答请求看到「流程分步引导策略」，策略互相干扰
3. 维护时改流程策略可能影响制度问答

**改造后：**

```
RagAgentConfiguration（基础 Prompt ~40 行）
  ├── 角色定义
  ├── 工具声明
  ├── 【最高优先级】规则
  ├── 【核心约束】禁止编造
  ├── 建议问题格式
  └── 通用规则

RagQaMessageHook（策略注入）
  ├── PROCESS_GUIDE → 流程分步引导策略（完整指令）
  ├── 五种事实查询 → 精准回答策略
  └── FALLBACK → 兜底检索策略
```

**量化收益：**
- 每次调用节省 ~60% prompt token（100行 → 40行基础 + ~20行策略）
- 制度问答不会被「请问您准备好了吗？我们从第①步开始」污染
- 修改流程策略只需动 `PROCESS_GUIDE_STRATEGY` 常量，不碰基础 Prompt

**这是一个通用的 Prompt 工程模式**：当 Agent 有多种行为模式时，将公共规则保留在 System Prompt 中，行为策略按上下文动态注入。

---

### Q9：项目中有一个「最高优先级指令」机制——工具返回 `【系统指令-最高优先级】` 时，LLM 必须原样输出。为什么需要这个机制？有没有更好的替代方案？

**参考回答：**

**为什么需要：**

LLM 的核心约束是「禁止编造」，但当 `searchKnowledge` 返回空结果或低质量结果时，LLM 可能出于「helpful assistant」的本能去编造答案。工具返回的最高优先级指令是一个**兜底安全机制**：

```
【系统指令-最高优先级】
知识库中未找到任何与「XXX」相关的文档内容。

回复模板：
抱歉，我未能在知识库中找到与您问题相关的信息。

建议您：
1. 尝试更换关键词重新提问
2. 联系 HR 部门获取人工帮助
```

**为什么用字符串模板而不是结构化控制：**
- 当前架构中工具返回值是字符串，Agent 框架不直接支持「工具返回指令强制 LLM 行为」
- 在 System Prompt 中约定「以 **【系统指令-最高优先级】** 开头时必须原样输出」是务实方案

**更好的替代方案（如果在更强的 Agent 框架中）：**

1. **工具返回结构化指令**：工具返回 `{type: "NO_RESULTS", template: "..."}`，Agent 框架解析后直接终止本轮推理
2. **Human-in-the-loop**：低质量结果时挂起等待人工确认
3. **条件路由（Graph-based Agent）**：检索后根据质量评分走不同分支，质量不足直接走兜底回复节点，不经过 LLM

**当前方案的价值**：实现简单，不需要框架升级，在 Spring AI + ReAct Agent 架构下是最轻量的安全兜底。







## 五、Embedding 与向量检索

### Q10：制度文档入库时做了智能切分（HeadingChunker / ProcessChunker / FaqChunker），为什么不能用通用的 TokenTextSplitter 一刀切？

**参考回答：**

**制度文档的结构特征：**

| 文档类型 | 结构特征 | 切分需求 |
|---------|---------|---------|
| 制度文档 | 层次化标题（第X章 → 第Y节 → 第Z条） | 按标题层级切分，保留章节路径元数据 |
| 流程文档 | "第①步 → 第②步" 步骤序列 | 按步骤切分，保留角色/时限/材料元数据 |
| FAQ 文档 | Q&A 对 | 按问答对切分，保留问题作为检索 key |

**TokenTextSplitter 的问题：**

TokenTextSplitter 按固定 token 数切分，不感知文档结构。会导致：
- 一个条款被切成两半："年假天数为...（此处被切断）...5天"
- 相邻步骤混在一起："第①步提交申请第②步部门审批" 被切成一个 chunk
- 丢失层级关系：检索到的 chunk 没有章节路径，无法标注出处

**智能切分的实现：**

```java
// chunkSmart() 的三级路由：
// 1. category 路由：制度→HeadingChunker, 流程→ProcessChunker, FAQ→FaqChunker
// 2. 自动检测：依次尝试三种切分器，看哪种命中结构标记
// 3. 兜底：TokenTextSplitter
```

切分后每个 chunk 携带结构化元数据：
- 制度：`heading_path = "第5章 休假制度 > 第5.2条 年假计算"`
- 流程：`step_title = "第①步"`, `step_role = "部门经理"`, `step_time_limit = "1个工作日"`
- FAQ：`qa_question = "年假怎么计算？"`

这些元数据直接用于：
1. 检索结果的【出处】标注
2. 结构去重（同文档同章节只保留最高排名的 chunk）
3. 前端展示（版本追溯、引用定位）

---

## 六、Skill / MCP 概念

### Q11：这个项目使用了 Spring AI 的 Skills 机制（external-skills 目录）。Skills 和 MCP（Model Context Protocol）有什么关系？在这个项目中 Skills 解决了什么问题？

**参考回答：**

**Skills 和 MCP 的关系：**

| | Skills（本项目用法） | MCP（Model Context Protocol） |
|---|---|---|
| **定位** | 本地文件系统中的 Prompt/工具定义 | 标准化的客户端-服务器协议 |
| **范围** | 项目内、文件维度 | 跨进程、跨服务 |
| **工具注册** | 文件系统扫描 + `SkillRegistryConfiguration` | MCP Server 暴露 tools/list 端点 |
| **发现机制** | 静态配置 `skills.file-system.path` | 动态发现（SSE / stdio 传输） |

**类比**：Skills 是本地的「静态 MCP」——定义了工具的行为和 Prompt 模板，但不走网络协议。MCP 是「动态 Skills」——标准化的工具暴露和调用协议，支持远程服务。

**本项目 Skills 解决的问题：**

```yaml
skills:
  file-system:
    path: ./external-skills
```

`FileSystemServerTools` 提供了文件系统操作工具（读/写/列目录），Agent 可以通过这些工具访问 workspace 目录中的文件。这实现了：

1. **能力扩展**：Agent 不仅会检索知识库，还能操作文件系统
2. **工具隔离**：文件操作工具在 external-skills 目录中定义，不耦合到核心 RAG 逻辑
3. **安全边界**：工作目录限制在 `app.workspace.path`，防止 Agent 访问系统文件

**如果升级为 MCP：**
- 可以把知识库检索、文件操作、FAQ 管理等分别封装为 MCP Server
- 前端或其他服务可以通过 MCP 协议标准化调用
- 支持工具的热插拔和动态发现

---

### Q12：如果让你设计一个 MCP Server 来增强这个系统，你会设计哪些 Tool？

**参考回答：**

**1. 知识库管理 MCP Server：**

```
Tools:
- upload_document(file, category, department) → 上传并入库
- search_knowledge(query, topK, department) → 混合检索
- get_document_versions(groupId) → 查看文档版本历史
- rollback_version(groupId, version) → 回退到历史版本
```

**2. FAQ 管理 MCP Server：**

```
Tools:
- search_faq(query) → FAQ 语义匹配
- create_faq(question, answer, category) → 新增 FAQ
- get_faq_candidates() → 获取高频候选问题
- get_faq_stats() → FAQ 命中统计
```

**3. 质量监控 MCP Server：**

```
Tools:
- get_quality_overview(days) → 质量概览
- get_low_rated_messages(limit) → 低评分消息列表
- get_blind_spots() → 知识盲区分析（用户常问但系统答不上来的问题）
- get_daily_trend(days) → 每日评分趋势
```

**为什么要拆成多个 Server：**
- 独立部署和扩缩容
- 不同团队可以独立维护
- 权限隔离（FAQ 管理可能只有 HR 能操作）

---

## 七、系统质量与评估

### Q13：如何评估这个 RAG 系统的回答质量？项目中有哪些质量保障机制？

**参考回答：**

**项目中的质量保障机制：**

**（1）检索质量评分（`QualityScore`）：**

```java
// 综合 RRF 融合分数和 LLM 重排序分数
if (llm_score >= 3 && rrf_score >= 0.01) → PASSED
```

质量状态分三档：
- `PASSED`：至少一条有效召回 → 正常生成回答
- `LOW_QUALITY`：有结果但评分不足 → 「最高优先级指令」要求 LLM 如实告知
- `EMPTY`：无结果 → 直接返回「未找到」模板

**（2）用户反馈机制：**

```java
POST /agent/sessions/{messageId}/feedback
{ "rating": 1 }   // 赞
{ "rating": -1 }  // 踩
```

`QualityService` 基于反馈数据生成：
- `QualityOverview`：总体质量指标
- `LowRatedMessage`：低分消息列表（用于 badcase 分析）
- `BlindSpotItem`：知识盲区（高频无结果查询）

**（3）RAGAS 风格的评估维度（建议补充）：**

| 维度 | 含义 | 计算方式 |
|------|------|---------|
| Faithfulness | 回答是否忠于检索结果 | LLM 逐句验证 |
| Answer Relevance | 回答是否切题 | LLM 评估 |
| Context Relevance | 检索结果是否相关 | LLM 评估 |
| Context Recall | 检索是否覆盖了必要信息 | 人工标注 |

**（4）FAQ 闭环机制：**

- `getFaqCandidates()` 从聊天记录中挖掘高频问题
- 语义聚类（贪心质心）+ 覆盖过滤
- HR 可以将候选转为正式 FAQ → 后续同类问题零成本精确匹配

**当前不足：**
- 缺少自动化回归测试集（Golden Dataset）
- 质量评分只评估检索，不评估生成内容的忠实度
- 缺少 A/B 测试框架来验证 Prompt 变更的效果

---

## 八、系统设计与问题排查

### Q14：我们之前发现系统会「乱回答」，你做了哪些修复？背后的根因是什么？

**参考回答：**

**发现的 5 个关键问题：**

1. **FALLBACK 策略语义错误**（最致命）
   - 根因：设计文档中 FALLBACK 的策略是「反问澄清 + 禁止检索」，但 FALLBACK 的语义应该是「正则未命中时兜底检索」
   - 后果：~30-40% 的合法问题（"年假有多少天"）被拒绝检索
   - 修复：改为「先检索 → 基于结果回答 → 结果为空时才如实告知」

2. **流程引导被闲聊检测打断**
   - 根因：「好的，继续」「下一步」等流程续接短语 ≤ 15 字符且不含业务关键词，被 ChitchatDetector 判定为闲聊
   - 修复：新增 `isInProcessGuidanceFlow()` 检测，流程上下文中短回复跳过闲聊检测

3. **意图分类优先级错误**
   - 根因：POLICY_QA 排在 PROCESS_GUIDE 前面，「加班制度怎么申请」匹配 "制度" 先于 "怎么" + "申请"
   - 修复：PROCESS_GUIDE 前移到 POLICY_QA 之前

4. **业务关键词覆盖不全**
   - 根因：缺少「试用期」「事假」「迟到」「旷工」等高频词
   - 修复：补充 13 个关键词

5. **基础 Prompt 和策略指令的冲突**
   - 根因：基础 Prompt 的「建议问题格式」和流程策略的「这一步清楚了吗？」可能同时出现
   - 修复：策略指令中增加排除规则（流程上下文不添加建议问题）

**排查方法论：**
- 不是从 LLM 输出反推问题，而是**逐节点检查 Pipeline 每个环节的输入输出**
- 重点检查「确定性组件」（正则、关键词匹配）是否正确，因为这些是可控的
- LLM 的输出不确定性是果，Pipeline 前面的路由错误才是因

---

### Q15：如果系统要支持 1000 并发用户，哪些环节会成为瓶颈？怎么优化？

**参考回答：**

**瓶颈分析（按严重程度排序）：**

| 环节 | 瓶颈类型 | 优化方案 |
|------|---------|---------|
| **LLM 调用** | 延迟最高（2-5s），并发受限 | ① FAQ 拦截（已知问题零 LLM 调用）；② 语义缓存（相似问题复用答案）；③ LLM 实例水平扩展 |
| **Embedding 调用** | FAQ 语义匹配每次调 API | ① FAQ embedding 缓存（已做）；② 批量调用（已做 batch=50）；③ 本地部署 embedding 模型 |
| **向量检索** | pgvector HNSW 索引，大数据量时退化 | ① 索引优化（ef_construction/ef_search 参数调优）；② Milvus 作为主检索库，pgvector 为备 |
| **数据库连接** | 连接池耗尽 | ① HikariCP 连接池调优；② 读写分离（检索读从库） |
| **SSE 连接** | CachedThreadPool 上限 | ① 虚拟线程（Java 21+）；② 异步非阻塞（WebFlux） |

**架构优化策略：**

1. **三级缓存体系**：
   - L1：FAQ 精确/模糊匹配（零延迟）
   - L2：语义缓存（相似度 > 0.95 的问题直接复用答案）
   - L3：RAG 检索 + LLM 生成（完整链路）

2. **异步化**：
   - 用户反馈、FAQ 候选挖掘、质量分析等离线任务异步处理
   - LLM 重排序和 embedding 调用用 CompletableFuture 并行化

3. **降级策略**：
   - LLM 不可用时 → 直接返回检索结果（搜索模式）
   - 向量检索超时 → 只用关键词检索兜底
   - FAQ 服务异常 → 跳过 FAQ 匹配，直走 RAG 链路

---

## 九、开放性问题

### Q16：如果要给这个系统加上「多模态」能力（比如员工上传请假条的截图，系统自动识别并告知后续步骤），你会怎么设计？

**参考回答：**

**架构设计：**

```
用户上传截图
  │
  ├─ ① OCR / 视觉理解（多模态 LLM 或专用 OCR）
  │    识别：请假类型、日期、原因等结构化字段
  │
  ├─ ② 意图推断（基于识别结果）
  │    "识别出请假类型=病假，日期=3天" → 意图 → 病假流程指引
  │
  ├─ ③ RAG 检索（标准链路）
  │    检索：病假制度条款 + 病假申请流程
  │
  └─ ④ 生成回答
       "根据您上传的病假条，您需要申请病假。根据公司制度：
        ① 提交医院开具的病假证明
        ② 提前1天在OA系统提交申请
        ..."
```

**技术选型：**
- 多模态模型：Qwen-VL（同生态）或 GPT-4V
- 结构化提取：Prompt 引导模型输出 JSON 格式
- 容错：OCR 识别不清时反问用户确认关键字段

**关键设计考量：**
- 不是简单地把图片传给 LLM，而是**先结构化提取再路由**，保持和文本链路一致的架构
- 图片内容不进入向量库（隐私 + 检索效率），只将提取的结构化信息入库

---

### Q17：你怎么看 Agent 框架的发展趋势？Spring AI、LangChain、LangGraph 各有什么优劣？

**参考回答：**

| 框架 | 定位 | 优势 | 劣势 |
|------|------|------|------|
| **Spring AI** | Java 生态的 AI 集成层 | Spring 生态原生集成（DI、AOP、事务）；与 pgvector/Redis 等中间件深度整合；生产级可观测性 | 社区较新，某些高级 Agent 模式（Map-Reduce、并行 Agent）支持不完善 |
| **LangChain** | Python AI 应用框架 | 生态最丰富；Chains/Agents/Tools 抽象灵活；社区庞大 | 抽象层次多，调试困难；过度封装导致"魔法"行为；生产环境稳定性待验证 |
| **LangGraph** | 有状态 Agent 工作流 | 图结构天然适合多步骤 Agent；状态管理明确；支持分支/循环/并行 | 学习曲线陡峭；Python only |

**本项目的选择逻辑：**
- 团队是 Java 技术栈 → Spring AI 是自然选择
- ReAct Agent 是 Spring AI 原生支持的 Agent 模式
- 如果有更复杂的 Agent 工作流需求（多 Agent 协作、条件分支），可以考虑 Spring AI + 自建简单图编排，或引入 LangGraph 作为 Agent 编排层

**趋势判断：**
- Agent 框架正在从「Chains（固定流程）」向「Graphs（动态流程）」演进
- MCP 协议可能成为 Agent 工具调用的标准，框架只需要支持 MCP 即可接入任意工具
- 评估（Evaluation）和可观测性（Observability）会成为 Agent 框架的核心竞争力

---

---

### Q18：你是怎么构思这个项目的？有设计文档（思路）吗？

**参考回答：**

有的，整个项目的构思遵循了「需求分析 → 架构设计 → 技术选型 → 迭代细化」的结构化流程。

**第一步：需求分析**

拿到需求文档后，我把它拆成 5 类能力需求：
- 制度问答（条款检索 + 精准回答）
- 流程指引（分步引导 + 多轮交互）
- 多轮对话（上下文理解 + 追问处理）
- 引用溯源（出处标注 + 版本追溯）
- 兜底处理（知识不足时的安全回复）

然后画了一个用户提问到系统回答的端到端流程，识别出 3 个关键决策点：**这个问法要不要走 FAQ？是不是闲聊？属于哪种意图类型？**

**第二步：架构设计（两轮迭代）**

我有两份正式设计文档：

1. **《意图路由优化设计》**
   - 发现了 `IntentType` 枚举定义了 6 种意图但从未被使用的问题
   - 设计了「Hook 标注 + LLM 决策」的路由模式
   - 确定了正则匹配（零成本）做意图分类，FALLBACK 做兜底

2. **《制度问答与流程指引分离设计》**
   - 发现单体 Prompt（~100行）中所有策略混在一起的问题
   - 设计了「基础 Prompt（公共规则）+ Hook 动态注入（具体策略）」的分离架构
   - 量化收益：每次调用节省 ~60% prompt token

这两份设计文档都在 `docs/superpowers/specs/` 目录下。

**第三步：技术选型**

| 层次 | 选择 | 考量 |
|------|------|------|
| 框架 | Spring Boot 3 + Spring AI Alibaba | 团队 Java 技术栈 |
| LLM | DashScope Qwen-Plus | 中文制度文档理解好，低温度 0.2 防幻觉 |
| Embedding | text-embedding-v2 (1536维) | 同生态配套，中文 C-MTEB 表现优 |
| 向量库 | pgvector (HNSW) + Milvus (备) | pgvector 开发成本低，Milvus 为大规模兜底 |
| Agent | ReAct Agent | Spring AI 原生支持，单工具场景足够 |
| 前端 | Vue +Element plus  UI             | 组件化，SSE 流式消费友好 |

**第四步：迭代验证**

设计文档写完不是终点。代码实现后我做了两件事：
1. **逐节点验证**：不是看 LLM 输出，而是逐节点检查 Pipeline 每个环节的输入输出是否正确（我们发现的正则覆盖、闲聊误判等问题都是这样排查出来的）
2. **Badcase 驱动迭代**：发现「年假有多少天」被 FALLBACK 反问后就立刻修了 FALLBACK 策略

> 以上回答结合了项目实际代码、设计文档和 AI 工程化最佳实践。面试中建议根据面试官的追问深度灵活调整回答的详细程度。
