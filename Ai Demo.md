```
**提交要求**：

- 完整的源代码。
- 部署和运行的说明文档。
- 一份演示报告，展示系统在制度问答、多轮追问、引用展示和无答案处理场景下的表现。

**附录：技术参考**
文档处理
- PyPDF2 / pdfplumber：用于处理PDF制度文档。
- python-docx：用于处理Word格式的制度和流程文件。
- pytesseract / EasyOCR：用于识别扫描件中的文字内容。
检索与问答
- LangChain / LlamaIndex：用于构建检索增强问答流程。
- FAISS / Chroma：用于向量检索和知识库索引。
- BGE / text-embedding-3 等Embedding模型：用于文档向量化。
- LLM API：用于问题理解、答案生成和多轮问答。
数据处理
- Pandas：用于处理FAQ、文档元数据和辅助结构化数据。
- regex：用于提取条款编号、流程节点和关键字段。
可视化与应用
- Streamlit / Dash：用于构建制度问答和流程指引界面。
```



```
构建一个公司制度知识库问答与流程指引助手，支持员工基于员工手册、请假制度、考勤规范、报销流程、入离职流程和常见FAQ进行自然语言问答。系统需要围绕基础知识库构建与检索增强问答展开，能够对制度文档进行切分、向量化、索引和检索，并结合大语言模型生成有依据的回答。系统应重点体现向量检索、RAG问答、多轮对话和引用溯源等基础能力，而不是构建复杂的知识平台或完整业务系统。

本题更强调知识库问答原型的可用性，要求（建议）实现复杂的混合检索、重排序、或企业级知识治理能力，这些内容可以作为扩展项。
```



- 前端
  -  Node.js（版本 18 或 20 LTS）
  -  cd C:\IdeaProject\aidemo\frontend
      npm install    # 安装依赖（如果还没装过）
      npm run dev    # 启动开发服务器



搜索

- （向量 + pg_trgm 关键词 + RRF
    融合

- 






 最欠缺的模块（按优先级排序）

    1. 反馈评分系统 — 用户无法对答案进行 👍/👎 评价。没有反馈闭环，就不知道哪些回答质量差、哪些知识库内容需要更新。这是知识库产品最基础的机制。

    2. （未完成）流程分步指引 — 系统自称"流程指引助手"，但实际的流程指引只是单轮 RAG 问答。真正的流程指引应该是分步骤的交互式引导（如请假：①确认资格 → ②选择类型 → ③提交材料清单
       → ④等待审批），每一步有明确的指引和材料要求。

    3. 答案质量监控 — 当前统计只有 FAQ 命中次数。缺少：用户满意度趋势、未被回答的高频问题排行、知识盲区识别、回答质量的宏观视图。

    4. 知识版本与变更追溯 — 文档有 version 字段但没有版本历史。企业制度频繁更新，员工需要知道"这个政策是什么时候改的？改了哪些内容？"
       -  两个独立文档 — 分别上传，文件名可能是「年假制度_2025」和「年假制度_2026」，用户手动管理，系统不做关联

    5. 部门/组织知识隔离 — 系统有 department 字段但没有做内容权限隔离。HR 制度、IT 制度、财务制度应该按部门区分可见性。




具备的功能

1. 复制系统的答案
2. 反馈评分系统
   - 用户无法对答案进行 👍/👎 评价。就没有反馈闭环，就不知道哪些回答质量差、哪些知识库内容需要更新。这是知识库产品最基础的机制
3. 答案质量监控 — 当前统计只有 FAQ 命中次数。缺少：用户满意度趋势、未被回答的高频问题排行、知识盲区识别、回答质量的宏观视图。
4. 部门知识隔离
   - HR 制度、IT 制度、财务制度应该按部门区分可见性。



1. 系统目前是乱回答的，知识库如果没有召回信息，就不要乱回答了
2. 没有给出流程指引
   - 流程分步指引：系统自称"流程指引助手"，但实际的流程指引只是单轮 RAG 问答。真正的流程指引应该是分步骤的交互式引导（如请假：①确认资格 → ②选择类型 → ③提交材料清单
     → ④等待审批），每一步有明确的指引和材料要求。







查询重写

- 本质解决的问题：用户口语 与 文档书面语的词表鸿沟
- 市面常见改写方式
  - 一条问题N个变体，分别检索，合并去重
  - 先生成一份假象的答案文本，用答案的向量去搜
  - 把具体问题抽象化，扩大检索面
- 为什么没有：系统采用了三层补偿
  - 系统采用向量语义检索（embedding相似度）（天然跨过用词差异）
  - pt_trgm关键词 字符三元组模糊匹配（部分补偿）
  - LLM重排序 相关性打分（兜底）
  - 如果加一层LLM改写（延迟翻倍，且收益少）
    - 收益少：用户用了知识库里完全不存在的同义词，且 embedding 恰好没捕捉到

